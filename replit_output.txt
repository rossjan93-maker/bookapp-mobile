================================================================================
P2C — Soft-Avoid Retrieval Deprioritization — COMPLETE
================================================================================
Date: 2026-05-14
Approved scope: Gaps A, C, D, E (Gap B / revealedAuthors deferred to P4)
Protocol: Strict TDD (tests-first), ≤2 auto-recovery attempts per failure
DB schema/migrations: untouched
P2B.1 retrieval-provenance AND-gate: preserved verbatim
6-phase pipeline geometry: preserved

--------------------------------------------------------------------------------
FILES TOUCHED (7)
--------------------------------------------------------------------------------
  M  lib/recPolicy.ts                              (+LIKED_SUBJECT_AVOID_GUARDS, doc updated)
  M  lib/retrieval/branchPlanner.ts                (sparse trigger + lanesDeprioritized flag passthrough)
  M  lib/retrieval/branches/revealedLanes.ts       (+softAvoidedTopGenres, +liked-subject guard, +deprioritizedFlag)
  A  lib/retrieval/softAvoidLocal.ts               (NEW — applyLocalSoftAvoidFilter + classifyCandidateAvoidKey)
  M  lib/recommender.ts                            (wired filter at primary + fallback; meta surfaced; RetrievalTrace extended)
  M  scripts/validate_retrieval_planner.ts         (+A7a/b/c/d/e/f/g sections — 19 new assertions)
  M  replit.md                                     (P2B.1 entry extended → P2C)

--------------------------------------------------------------------------------
TDD TIMELINE
--------------------------------------------------------------------------------
RED (Step 1 verification, before implementation):
  Error: Cannot find module '../lib/retrieval/softAvoidLocal'
  → confirmed validator could not even load → tests fail by import.

GREEN (Step 3 final, after implementation):
  retrieval planner          OK — 0 failure(s)   (56 prior + 19 P2C = 75 PASS)
  stated reservation         OK — 0 failure(s)   (56/56, P2B.1 unchanged)
  taxonomy                   OK — 21 defs / 53 aliases / 18 edit / 17 intake / 17 legacy
  recValidity                OK — hash determinism, tolerance, configHash semantics
  tsc --noEmit               0 errors in lib/{recommender,recPolicy,retrieval,recRequest}
                             (pre-existing app/(tabs)/* PostgrestSingleResponse errors unrelated)

Auto-recovery used: 1 (broke RetrievalTrace closing brace on a multi-line edit;
fixed in next turn). 0 second-attempts needed.

--------------------------------------------------------------------------------
GAP-BY-GAP MAPPING
--------------------------------------------------------------------------------
Gap A — sparse-user trigger (topGenres ∩ softAvoids)
  Where: lib/retrieval/branchPlanner.ts step 3 + softAvoidedTopGenres()
  Tests: A7a (trigger fires, quota cut, anchor masked) + A7b (no-intersect → no cut)

Gap C — liked_subject guard list
  Where: LIKED_SUBJECT_AVOID_GUARDS in lib/recPolicy.ts
       + isLikedSubjectAvoided() in lib/retrieval/branches/revealedLanes.ts
  Tests: A7c (epic-fantasy dropped when fantasy_scifi avoided; family-saga survives)

Gap D — local-candidate soft-avoid filter
  Where: lib/retrieval/softAvoidLocal.ts (pure helper, deterministic)
       + wired in lib/recommender.ts after getLocalCandidates AND after the
         catalog-fallback scan
  Behavior: keep ceil(N * (1 - SOFT_AVOID_RETRIEVAL_MULTIPLIER)) of avoided;
           non-avoided pass through; first-seen-wins ordering
  Tests: A7e (classifyCandidateAvoidKey) + A7f (filter ratio + determinism + no-op)

Gap E — observability
  Where: FetchItem.softAvoidDeprioritized (already in types.ts) populated by
         buildRevealedLanesBranch when planner sets deprioritizedFlag;
         RetrievalTrace.soft_avoid_retrieval { avoidsApplied, localDemoted,
         branchQuotaMultiplier } populated in getCandidateBooks
  Tests: A7d (per-FetchItem flag) + A7g (P2B.1 stated_genre: prefix preserved
         AND statedGenres items NOT flagged)

Gap B — revealedAuthors deprioritization: DEFERRED to P4 (per approved scope)

--------------------------------------------------------------------------------
INVARIANTS HELD
--------------------------------------------------------------------------------
✓ Soft avoid demotes — never excludes (multiplier=0.5; quota floored at 1)
✓ Determinism: identical inputs → identical kept[] order across runs
✓ statedGenres branch untouched: every item still emits `stated_genre:` prefix
✓ statedGenres items never carry softAvoidDeprioritized=true
✓ P2B.1 retrieval-provenance AND-gate operates unchanged (gate keys off
  `_retrieval_reason.startsWith('stated_genre:')` AND positive contribution)
✓ Empty softAvoids → no-op (kept === input, demotedCount === 0)
✓ Pre-P2C dense behavior preserved when softAvoids empty
✓ No DB writes / migrations / schema changes
✓ Subject classification confined to LIKED_SUBJECT_AVOID_GUARDS — no LLM, no
  fuzzy matching beyond curated substrings

--------------------------------------------------------------------------------
HONEST LIMITATIONS / FOLLOW-UPS
--------------------------------------------------------------------------------
- Subject coverage in `books.subjects` remains the bottleneck for local filter
  precision. The classifier returns null for any candidate without subjects,
  so soft-avoid demotion is naturally bounded by enrichment coverage.
  (Tracked separately under "Subject coverage batch repair" follow-up.)
- LIKED_SUBJECT_AVOID_GUARDS is curated (7 keys). Adding more keys is a
  one-line policy edit — no architectural change required.
- Gap B (revealedAuthors) deferred to P4. Author-level soft-avoid signal does
  not exist in the current taxonomy; closing it requires a hard-avoid storage
  decision per the architecture spec.
- Local filter has not been walked end-to-end against a live account yet
  (consistent with existing P2B.1 verification gap).

================================================================================
RESULT: GREEN — P2C ready for hand-off to P3 (contribution scoring +
explanation faithfulness). Quality gates A1–A7 satisfied; A8–A10 remain in
P3 scope.
================================================================================
