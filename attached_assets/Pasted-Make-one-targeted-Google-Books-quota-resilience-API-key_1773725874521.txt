Make one targeted “Google Books quota resilience + API key support” pass. Do not change package versions. Do not add packages unless absolutely necessary. Do not broaden this into another generic metadata pass. Do not refactor unrelated app logic. Confirmed current state: For Glow (The Plated Prisoner, #4), the metadata pipeline now reaches the Google Books fallback correctly, but both Google Books queries return HTTP 429. This means: - matching/path logic is now working - persistence is not the blocker - Google Books rate limiting/quota is now the blocker for cover_url + description Goal: Make the Google Books metadata path reliable enough for the app to populate cover and description when Google Books is the remaining source of truth. Scope: Only inspect and edit what is necessary, likely: - lib/googleBooks.ts - lib/debugRepairBook.ts - maybe lib/metadataRepair.ts if needed - maybe app/import/goodreads.tsx only if progress/status copy needs a rate-limit note - no unrelated broad rewrites Primary objectives A. Add Google Books API key support Requirements: 1. Support an optional env var for Google Books requests, such as: - EXPO_PUBLIC_GOOGLE_BOOKS_API_KEY 2. If present, include the key in all Google Books API requests. 3. If absent, keep current behavior as fallback. B. Handle 429s explicitly Requirements: 1. Detect HTTP 429 responses in Google Books helper(s). 2. Treat them as a rate-limit/quota condition, not a generic failure. 3. Log this explicitly in the debug tool. C. Add bounded retry/backoff Requirements: 1. For 429s, add a small bounded retry strategy where appropriate. 2. Keep it practical and conservative. 3. Do not create an aggressive retry loop. D. Reduce repeated wasteful attempts Requirements: 1. Avoid hammering Google Books repeatedly for the same incomplete book in tight loops. 2. Add a practical cooldown / retry-later approach if useful. 3. Keep this simple and explicit. E. Improve debug visibility Requirements: 1. In the Glow debug repair log, make it explicit when the failure is quota/rate-limit. 2. Show whether an API key is present. 3. Make the final verdict clear: - “Google Books quota/rate limit blocked cover/description retrieval” F. Keep behavior graceful Requirements: 1. If Google Books is rate-limited, fail quietly in the normal user flow. 2. Do not break Library or Book Detail. 3. But make the debug/dev path explicit. Implementation guidance 1. This is not a metadata redesign. 2. This is a quota/reliability fix for the remaining Google Books dependency. 3. Keep the code explicit and easy to reason about. 4. Do not assume Google Books always succeeds. 5. Preserve current OL behavior unchanged. After editing, print only: 1. files changed 2. full contents of changed files 3. the diff for those files only 4. exact manual test steps for Glow 5. Google Books quota-handling rules used 6. exact root cause confirmed 7. exact fix applied 8. what still remains for this title after the pass