We are moving from diagnosis to implementation. This pass is NOT a redesign pass. Do NOT do broad UI changes. Do NOT revisit copy unless required for a state/error surface. This pass is about fixing the highest-leverage integrity issues exposed by the checkpoint. ================================================== OBJECTIVE ================================================== Implement the core state-hygiene and recommendation-trust fixes that are still compromising the product: 1. Clear stale local onboarding / rec-entry state correctly 2. Make the onboarding-questions path durably complete onboarding 3. Add a real timeout + error path to recommendation generation 4. Tighten debugging on RecEntryScreen decisioning so regressions are easier to catch The goal is to reduce lifecycle fragility and stop state drift across: - sign out - account delete - dev reset - same-device recreate - cross-device onboarding continuation - poor network conditions during recommendations loading ================================================== PRIORITY 1 — CLEAR LOCAL STATE PROPERLY ================================================== Problem: AsyncStorage / localStorage keys are surviving too long and corrupting future user journeys. The checkpoint identified this as the biggest regression engine. I want a single, explicit clearing strategy for all onboarding / rec-entry local state. At minimum, audit and clear the relevant keys on: - SIGNED_OUT - successful account deletion - dev onboarding reset - any other “fresh start” flow that is meant to simulate a new user Keys to audit and clear appropriately include: - readstack_onboarding_stage_v1 - readstack_walkthrough_v1 - readstack_rec_entry_v1 - readstack_guided_tour_v1 if still legacy / in use - any onboarding-question or import-related local keys - any rec payload/session keys that can mislead a recreated user if they should not survive Before coding: 1. List every local key you believe should be cleared for a true fresh-start user state 2. Tell me exactly which flows will clear which keys 3. Tell me if any keys should intentionally survive sign-out and why Implementation requirement: Do not scatter this logic everywhere. Centralize it in a small reusable utility if appropriate. ================================================== PRIORITY 2 — MAKE THE QUESTIONS PATH DURABLY COMPLETE ONBOARDING ================================================== Problem: The intake/questions path appears to rely on local stage resolution, but it is still unclear whether it always writes durable DB onboarding completion at the correct point. I want this fixed and made explicit. Required behavior: - entering onboarding questions should NOT itself mark onboarding complete - finishing the questions flow should mark onboarding complete durably - abandoning the questions flow mid-way should behave intentionally and predictably - cross-device behavior should not be inconsistent because DB completion was never written Before coding: 1. Trace the exact current path: onboarding-import -> questions -> completion 2. Tell me whether `markOnboardingComplete()` is currently called, where, and when 3. Tell me what happens today if the user exits the questions flow mid-way 4. Tell me the corrected durable state model for this path Implementation requirement: The completion point for the questions path must be explicit and durable. Do not rely only on local stage = done. ================================================== PRIORITY 3 — ADD A REAL PIPELINE TIMEOUT + ERROR STATE ================================================== Problem: Recommendations can still sit in “building your picks” / loader states too long on weak connections because the pipeline has no hard timeout / graceful failure state. I want a real timeout and recovery path. Required behavior: - recommendation generation should not hang indefinitely - if the network/pipeline times out, clear the misleading loading state - show an honest fallback state - do not leave the user staring at a loader forever Before coding: 1. Identify the exact long-running fetch/pipeline path(s) 2. Tell me where the timeout should be added 3. Tell me what user-visible state should render on timeout 4. Tell me how this differs from: - low signal - quality gate - true active loading Implementation requirement: - use a real timeout/abort pattern where possible - always clear loading state in failure/timeout paths - log timeout distinctly so it is visible in dev diagnostics ================================================== PRIORITY 4 — ADD BETTER RECOMMENDATIONS ENTRY DECISION LOGGING ================================================== Problem: RecEntryScreen show/skip behavior is still hard to reason about from logs alone. I want explicit dev logging for the decision path. Add clear __DEV__ logs for: - onboarding stage - hasSeenRecEntry result - hasPersonalizationSignal result - final maybeShowEntry decision - whether the hub is shown instead - whether recommendations are withheld due to tier/quality gate/timeout These logs should be: - specific - not spammy - easy to grep ================================================== IMPORTANT CONSTRAINTS ================================================== Do NOT use this pass to: - redesign final setup UI - redesign coach bubble UI - change onboarding copy broadly - re-architect the recommender - touch unrelated app surfaces This pass is about correctness, lifecycle hygiene, and trustworthy state behavior. ================================================== WHAT I WANT BEFORE CODING ================================================== Before implementing, give me: 1. Exact files/components/state you will modify 2. Exact local keys you will clear and in which flows 3. Exact current vs corrected onboarding-questions completion behavior 4. Exact timeout/error approach for the recommendations pipeline 5. Exact new debug logs you will add Then implement. ================================================== SUCCESS CRITERIA ================================================== - same-device account recreation no longer skips or corrupts onboarding - sign-out and delete no longer leave stale onboarding / rec-entry state behind - onboarding-questions completion writes durable onboarding completion correctly - abandoning questions mid-flow behaves intentionally - recommendations pipeline cannot hang indefinitely in loading - timeout/error states are honest and distinct from low-signal / quality-gated states - RecEntryScreen decisioning is easy to trace in dev logs - no broad regressions to the current onboarding sequence