Your analysis is right. I want to proceed with the safest beta path: implement Part 1 and Part 2 now, and defer the WebView assistant for later. Decision: - Do implement native document picker support - Do implement pasted raw CSV text support - Do NOT build the Goodreads WebView assistant yet - Do NOT spend time on download interception Why: The goal for beta is to remove the mobile dead end and remove dependence on the browser behaving nicely. A reliable import path is more important than a clever one. ================================================== OBJECTIVE ================================================== Fix the Goodreads import acquisition flow so users can complete import even when: - they are on mobile - the CSV does not download cleanly - Goodreads opens the export as raw text in the browser I want the app to support three visible acquisition paths: 1. Choose CSV file 2. Paste CSV text 3. Open Goodreads export page The first two are the real import paths. The third is just the external acquisition step. ================================================== IMPLEMENT NOW ================================================== PART 1 — Native document picker Add native document picker support so mobile users can pick a Goodreads CSV from Files/Downloads instead of being told to switch to web/desktop. Requirements: - work on native/mobile - use the existing parser path - no separate parsing logic - no dead-end messaging telling users to leave the app PART 2 — Paste raw CSV text Add a clear fallback path where users can paste raw CSV text directly into readstack. Requirements: - visible on the import screen - clearly positioned as a fallback when Goodreads opens raw CSV text in browser - route pasted text through the same parseGoodreadsCSV path if possible - validate it clearly - if the pasted text is not valid Goodreads CSV, show a clean error message ================================================== DO NOT IMPLEMENT YET ================================================== Do NOT build: - WebView Goodreads assistant - download interception - Goodreads login/session tooling - fragile automation based on browser behavior That can wait until later. ================================================== UX REQUIREMENTS ================================================== The import screen should now clearly present: Primary: - Choose CSV file Secondary: - Paste CSV text Supporting: - Open Goodreads export page I want the user to understand immediately: - how to get the export - how to bring it into readstack - what to do if the browser opens raw CSV text instead of downloading a file Remove the current mobile dead-end language. No more instructions that effectively tell users to leave the product and use a different environment as the only path. ================================================== BEFORE CODING ================================================== Before implementing, tell me: 1. exact files/components/routes you will modify 2. how the document picker path will work 3. how the pasted-text path will work 4. whether both paths can reuse the existing parser cleanly 5. what the revised screen hierarchy will be 6. any package/install/config changes required Then implement. ================================================== SUCCESS CRITERIA ================================================== - mobile users are no longer blocked by the current dead-end flow - users can import via CSV file OR pasted CSV text - the app no longer depends on the browser exposing a save/download option - the import screen is clearer and more resilient - no parser/data-layer regressions are introduced