We need to remove the brief "Unmatched Route" flicker that appears when the user taps the Supabase confirmation email link. ================================================== CURRENT BEHAVIOR ================================================== The email link opens: readstack://auth/callback?code=... The app eventually routes correctly, but before that it briefly shows Expo Router's "Unmatched Route" screen. So the callback flow works functionally, but the UX is wrong. ================================================== GOAL ================================================== Make the auth callback feel intentional and seamless. Instead of showing "Unmatched Route", I want a real callback route/screen that handles the deep link and shows a lightweight loading state such as: "Signing you in…" ================================================== WHAT TO IMPLEMENT ================================================== 1. Add a real Expo Router route that matches: /auth/callback 2. That route should: - render a minimal loading screen - process the incoming auth deep link / code - create or resume the Supabase session - route the user to the correct next destination (likely onboarding for a new user, or the main app for a returning user) 3. Remove the visible dependency on the router falling through to "Unmatched Route" before the global deep link handler runs. 4. Keep the existing deep link/session logic consistent, but move or coordinate it so the callback route becomes the visible entry point for: readstack://auth/callback ================================================== IMPORTANT ================================================== I do NOT want: - a generic 404/unmatched page flash - a blank screen - a browser fallback I DO want: - a dedicated callback route - a clean loading state - deterministic routing after auth completes ================================================== BEFORE CODING ================================================== Tell me: 1. how the current unmatched-route flicker is happening 2. what callback route file you will add 3. whether you will move logic from the global deep-link handler into that callback screen, or coordinate them another way 4. what final routes users will land on after successful callback for: - new user - existing user Then implement. ================================================== SUCCESS CRITERIA ================================================== - tapping the confirmation email link no longer shows "Unmatched Route" - the app shows a clean "Signing you in…" state instead - auth callback completes successfully - the user is routed to the correct next screen