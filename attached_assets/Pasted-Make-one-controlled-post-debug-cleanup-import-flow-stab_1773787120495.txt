Make one controlled “post-debug cleanup + import flow stabilization” pass. Do not change package versions. Do not add packages unless absolutely necessary. Do not refactor unrelated recommendation, profile, Home, pacing, or social logic. Do not break Goodreads import, reset/reimport, library, book detail, or reading flows. Context: The Goodreads import + metadata repair path is now working. The temporary debug/dev tooling used to diagnose Glow has served its purpose. We now need to clean up the app and leave only production-appropriate behavior. Goal: 1. remove temporary debug/dev repair tooling from the user-facing app 2. keep the polished import/loading flow and “Go to Library” CTA 3. preserve the working metadata repair behavior 4. remove any temporary one-off debug UI/logging that should not remain in production Scope: Inspect and edit only what is necessary, likely: - app/settings.tsx - lib/debugRepairBook.ts - maybe any temporary debug imports/usages - app/import/goodreads.tsx only if needed for small production polish - no unrelated broad rewrites Primary objectives A. Remove temporary Dev Tools UI Requirements: 1. Remove the temporary “Dev Tools” section from the in-app Settings screen. 2. Remove the temporary “Repair Glow” trigger from the user-facing UI. 3. Leave Settings clean and production-appropriate. B. Remove temporary single-book debug helper usage Requirements: 1. Remove any temporary imports/usages of the one-off Glow debug repair helper from production UI paths. 2. If lib/debugRepairBook.ts is no longer needed, remove it. 3. If you prefer to keep it for local/internal development, ensure it is not reachable in the production app UI. C. Preserve working metadata behavior Requirements: 1. Do not undo the fixes that made imported metadata work. 2. Keep: - OL identifier repair - title normalization / multi-variant search - Google Books fallback - metadata persistence 3. Cleanup should not reduce metadata completeness. D. Preserve import UX improvements Requirements: 1. Keep the improved full-screen import/loading experience. 2. Keep the “Go to Library” CTA on import completion. 3. Keep the current summary/results behavior unless a tiny cleanup is beneficial. E. Remove temporary noisy debug logging where appropriate Requirements: 1. Remove or quiet any temporary one-off debug logging that was only added for the Glow forensic pass. 2. Keep only production-useful logging or silent fail behavior. 3. Do not leave verbose single-book trace noise in normal app flows. F. Keep the app production-clean Requirements: 1. Settings should feel intentional again. 2. Goodreads import should feel like a polished user feature, not an engineering sandbox. 3. No debug-only affordances should remain visible to normal users. Implementation guidance 1. Keep this a cleanup pass, not a redesign. 2. Make the smallest set of changes that remove the temporary debugging footprint. 3. Preserve all validated working behavior. 4. Be careful not to delete shared helpers that are still required by the real metadata pipeline. After editing, print only: 1. files changed 2. full contents of changed files 3. the diff for those files only 4. exact manual test steps 5. what debug tooling was removed 6. what production behavior was preserved 7. any temporary tooling intentionally left in code but no longer user-visible