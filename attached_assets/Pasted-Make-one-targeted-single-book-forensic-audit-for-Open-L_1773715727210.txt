Make one targeted “single-book forensic audit for Open Library metadata persistence” pass using Lord Jim as the test case. Do not change package versions. Do not add packages unless absolutely necessary. Do not refactor unrelated import, library, profile, Home, pacing, or recommendation logic. Do not broaden this into another general metadata pass. Known facts: - In Supabase, the books row for Lord Jim has: - cover_url populated - page_count populated - external_id = /works/OL38684W - description = null - subjects = null - The database now has both books.description and books.subjects columns present. - Open Library data for Lord Jim exists, so this is no longer a missing-source-data problem. Goal: Identify the exact break in the metadata path for books that already have a valid Open Library external_id but still are not persisting description and subjects. Scope: Only inspect and edit what is necessary, likely: - lib/openLibrary.ts - lib/metadataRepair.ts - app/book/[id].tsx - maybe one small debug helper or temporary console logging if needed - no unrelated broad rewrites Primary objectives A. Audit the exact path for a book with: - external_id present - description null - subjects null B. Verify whether fetchOLMeta is actually being called Requirements: 1. Confirm the code path is reached for a book like Lord Jim. 2. If not, identify why not. C. Verify whether fetchOLMeta is parsing the Open Library response correctly Requirements: 1. Confirm what the raw Open Library response shape is for description and subjects. 2. Confirm the parser handles that shape correctly. 3. Fix any parsing mismatch. D. Verify whether description and subjects are being added to the DB patch Requirements: 1. Confirm the update payload actually includes them when found. 2. Confirm the update is sent to Supabase. 3. Confirm it succeeds. E. Verify Book Detail reads the persisted values Requirements: 1. If DB persistence is working, ensure the UI actually reads and renders description + subjects. 2. Fix any stale-state issue if present. F. Keep this narrow and conclusive Requirements: 1. In the response, explicitly state the single root cause found. 2. Do not answer with vague “improved metadata handling.” 3. Use Lord Jim as the concrete traced example. After editing, print only: 1. files changed 2. full contents of changed files 3. the diff for those files only 4. exact manual test steps using Lord Jim 5. the exact root cause found 6. the exact fix applied 7. whether this issue was parsing, persistence, or UI read/render