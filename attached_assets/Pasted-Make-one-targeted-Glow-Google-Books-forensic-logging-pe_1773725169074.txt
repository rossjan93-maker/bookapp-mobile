Make one targeted “Glow Google Books forensic logging + persistence audit” pass. Do not change package versions. Do not add packages unless absolutely necessary. Do not broaden this into another general metadata pass. Do not refactor unrelated app logic. Known current state for Glow (The Plated Prisoner, #4): - external_id is now a real Open Library work id - subjects and page_count have populated - cover_url is still null - description is still null - Open Library for the current work id does not provide cover or description - therefore the remaining missing fields must come from Google Books or another fallback - when testing by opening the book and waiting, the dev app refreshes and pushes back to Home, so Book Detail waiting is not a reliable validation path right now Goal: Use the existing dev repair path for Glow to conclusively trace the Google Books phase: 1. what exact title variants are queried 2. what raw Google Books results are returned 3. whether any result passes matching 4. whether a patch with cover_url / description is built 5. whether that patch is written to the DB successfully This is a narrow forensic debugging pass for one title. Scope: Inspect only what is necessary, likely: - lib/debugRepairBook.ts - lib/googleBooks.ts - maybe lib/metadataRepair.ts if needed - no unrelated broad rewrites Primary objectives A. Log the exact Google Books search flow for Glow Requirements: 1. For the Glow repair path, log each query variant attempted. 2. For each variant, log: - the exact query string - whether any items were returned - the top candidate titles/authors - whether the candidate passed or failed title matching 3. Make the logs explicit enough that we can see where the Google Books phase fails. B. Log the exact final patch decision Requirements: 1. If Google Books returns cover/description/page_count, log the exact values selected. 2. Log the final patch object before DB update. 3. Log the DB update result. 4. Log the final DB row after update. C. Confirm whether the issue is lookup failure or persistence failure Requirements: 1. In the response, explicitly state whether the problem is: - no usable Google Books result found - result found but rejected by matching - result accepted but DB patch not written - DB patch written but not persisted 2. Do not give a vague “improved metadata handling” answer. D. Keep testing on the dev repair button, not Book Detail waiting Requirements: 1. Do not rely on opening the book and waiting, since the dev environment is reloading. 2. Use the Settings dev repair path as the single source of truth for this forensic pass. Implementation guidance 1. Keep this temporary and surgical. 2. Prefer stronger logging over broader code changes. 3. We need a conclusive answer for Glow’s Google Books phase, not another speculative fallback layer. After editing, print only: 1. files changed 2. full contents of changed files 3. the diff for those files only 4. exact manual test steps for Glow 5. the exact Google Books queries/log checkpoints added 6. whether the issue is lookup failure, match rejection, or DB persistence 7. the exact root cause found 8. the exact fix applied, if any