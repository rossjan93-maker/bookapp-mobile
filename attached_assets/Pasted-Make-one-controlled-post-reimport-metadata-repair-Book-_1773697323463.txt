Make one controlled “post-reimport metadata repair + Book Detail user history” pass. Do not change package versions. Do not add packages unless absolutely necessary. Do not refactor unrelated recommendation, profile, Home, or pacing logic. Do not break Goodreads import, reset/reimport, library, or reading flows. Goal: Fix the gap where Goodreads reset + reimport succeeds structurally, but imported books still do not visibly feel complete in the product. We need two outcomes: 1. reimport should reliably trigger metadata repair for affected books 2. Book Detail should visibly show the user’s own imported/native history with that book Current problem: After using the Goodreads reset/reimport flow, the user still sees: - missing covers - missing about/description - missing subjects - no visible personal reading history on Book Detail Important clarification: The issue is not just “old account” state. The current pipeline is restoring/importing rows, but not reliably rehydrating book metadata or surfacing user memory back to the user. Scope: Only inspect and edit what is necessary, likely: - lib/goodreadsExecutor.ts - any metadata hydration helper(s) - app/book/[id].tsx - maybe app/import/goodreads.tsx if completion should trigger repair - maybe app/(tabs)/library.tsx only if needed for local cover updates - no unrelated broad rewrites Primary product objectives A. Reimport should trigger metadata repair for affected books Requirements: 1. After Goodreads import/reimport execution, run a bounded metadata repair step for all relevant affected books. 2. This must be broader than only `newBookIds`. 3. It should include books that were: - newly created - matched to existing rows - preserved across reset and then reimported 4. Metadata repair should target missing: - cover_url - description - subjects - page_count 5. Keep it bounded and safe, not an unbounded crawler. B. Use strong lookup priority Requirements: 1. Prefer: - isbn13 - isbn - external IDs - title + author fallback 2. Reuse and improve existing metadata helpers where possible. 3. Fail quietly if no metadata is found. C. Persist repaired metadata to books Requirements: 1. If metadata is found, persist it to the books table. 2. Do not rely only on local state. 3. Books should remain improved on future visits. D. Make Book Detail visibly personal Requirements: 1. Add a clean “Your History” / “Your Reading” section on Book Detail. 2. Surface, when available: - the user’s rating - finished date - review_body - private_note 3. Keep it elegant and compact. 4. If no user history exists, do not show an awkward empty block. E. Book Detail should remain self-healing Requirements: 1. When a user opens Book Detail for a book still missing metadata, it should still attempt hydration there too. 2. If hydration succeeds: - persist to books - update local state immediately F. Preserve reset/reimport safety Requirements: 1. Do not break the Goodreads reset logic. 2. Do not wipe preserved native state. 3. Reimport should still merge conservatively. 4. This pass should make reimported books feel complete, not change the reset semantics. G. Preserve all current flows Do not break: - Goodreads import - Goodreads reset - library status transitions - ratings - reviews - pacing - profile - Home - recommendations - book detail routing Implementation guidance 1. Treat this as a “rehydration and visibility” pass. 2. The main issue is not import itself; it is what happens after import. 3. Prefer one shared metadata repair path for import/reimport plus Book Detail fallback. 4. Keep the code explicit and easy to reason about. 5. If useful, return affected book IDs from the executor in a broader set than only newBookIds. Desired end state - reimported books gain covers/about/subjects more reliably - matched existing books are no longer skipped from metadata repair - preserved/reimported books can still be repaired - Book Detail shows the user’s own imported/native reading memory After editing, print only: 1. files changed 2. full contents of changed files 3. the diff for those files only 4. exact manual test steps 5. metadata-repair rules used 6. affected-book selection rules used 7. Book Detail user-history rules used 8. root cause found for why reset/reimport did not visibly improve books