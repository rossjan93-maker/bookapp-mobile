```text Make one controlled “personal library + richer book detail + future taste-fit foundation” pass. Do not change package versions. Do not add packages unless absolutely necessary. Do not add real AI API integrations yet. Do not refactor unrelated business logic. Do not break recommendation / credibility / activity flows. Do not build invite/share features in this pass. Goal: Turn the app into a stronger standalone reading product by: 1. letting users manually add and track their own books in their personal library 2. expanding the book detail screen so it feels materially useful 3. laying the groundwork for a future AI “will I like this?” / taste-fit feature without implementing the AI itself yet This pass should prioritize user value and clean UX. Files allowed to change: - app/(tabs)/library.tsx - app/(tabs)/search.tsx only if needed for shared book-search logic - app/book/[id].tsx - app/(tabs)/index.tsx only if needed for better book detail entry - app/(tabs)/notes.tsx only if needed for detail-page consistency - app/(tabs)/profile.tsx only if needed for detail-page consistency - add one new route/file if needed for a manual add flow - add one small shared helper/component file only if truly useful - schema/migration only if truly required for personal-library UX Primary product objectives A. Manual add to personal library Users must be able to add books for themselves, not only via recommendations. Requirements: 1. Add a clear CTA in Library for personal library growth: - examples: “Add Book”, “Add to Library” 2. Support a self-add flow that lets a user: - search for a real book and add it to their own library - if the book is not found, optionally add a simple custom/manual entry 3. When adding a book for self, allow choosing initial status: - want_to_read - reading - finished - dnf 4. Keep this self-add flow clearly separate from recommending a book to a friend. 5. The flow should feel simple and intentional, not like an admin workaround. 6. If a book already exists in the books table, reuse it. 7. If a user already has that book in user_books, do not create duplicates; handle this cleanly in the UI. B. Library should feel like a real personal tracker Requirements: 1. Preserve all current library status actions. 2. Keep recommendation-origin books working exactly as before. 3. Make the library feel useful for self-managed reading, not just received recommendations. 4. Improve the top of Library if helpful with: - a clear CTA - a short empty-state explanation - any small UX polish that supports self-tracking C. Expand the book detail screen The current detail screen is too thin. Turn it into a more useful reading surface. The detail page should support showing, when available: - cover - title - author - status - recommendation note - who recommended it - who you recommended it to - summary / description - lightweight subject / genre metadata if easily available - any personal-library context that is already available Requirements: 1. Book detail should feel richer and more complete than it is now. 2. If summary/description is not already in DB, use the smallest clean approach. 3. It is acceptable to fetch lightweight public metadata on the detail screen if that is the cleanest solution. 4. Do not overbuild a metadata ingestion pipeline. 5. Keep the visual style aligned with the current editorial reading-app direction. D. Taste-fit / AI foundation only I want a future feature where the app can explain whether a user may like a book based on their preferences. Do NOT implement real AI yet. Instead, create the right structural foundation. Requirements: 1. Create a clearly designed future section on book detail for something like: - “Why you might like this” - “Taste match” 2. This can be a placeholder or hidden-ready section, but it should fit the screen design. 3. If useful, add a very lightweight preference model foundation only if it is necessary and small. 4. Do not add LLM/API calls in this pass. 5. Do not fake AI output with generic text. E. Recommendation-specific detail improvements For books that came from recommendations, the detail screen should feel especially useful. Requirements: 1. If the detail view is opened from Inbox / Home / Profile and recommendation context is available, preserve and show that context cleanly. 2. Notes should feel deliberate and prominent enough to matter. 3. Status should be clear. 4. Do not remove existing context already being passed into detail. F. Keep all current flows intact Do not break: - recommendation sending - note sending - inbox lifecycle grouping - unread/new badge - library progression - credibility events - activity feed - profile tracker - friend detail screen - cover rendering and fallback logic Implementation guidance 1. If useful, create a dedicated manual-add route/screen rather than overloading Library inline. 2. Reuse the existing search/Open Library approach if it helps. 3. If manual/custom books require a schema addition, make the smallest safe migration only. 4. If metadata fetching for detail is added, keep it minimal and safe: - summary/description - maybe subjects - no giant ingestion pipeline 5. If custom/manual books are allowed, make sure the app can still render them cleanly even without external metadata. Important constraints - Keep this as a product-utility pass, not a giant architecture rewrite. - Prefer small, explicit changes. - Solve the highest-value UX first. - Do not build reviews, comments, club features, or invite flow here. After editing, print only: 1. files changed 2. full contents of all changed files 3. the diff for those files only 4. exact manual test steps 5. any schema or metadata tradeoff made 6. what was added now vs intentionally deferred for the future AI taste-fit layer ```