Make one controlled “friends list + reachable Friend Detail navigation” pass. Do not change package versions. Do not add packages unless absolutely necessary. Do not change schema. Do not refactor unrelated business logic. Do not broaden scope into a general social redesign. Do not break recommendation, friendship, activity, pacing, profile, or book-detail flows. Goal: Make the social/trust layer actually reachable by adding a clear accepted-friends list in the app and reliable navigation into Friend Detail. Problem: The audit found that app/friend/[id].tsx exists and contains meaningful trust/exchange logic, but it is hard or impossible to reach through normal app usage. That makes a real feature feel hidden and underpowered. Scope: Only inspect and edit what is necessary, primarily: - app/(tabs)/profile.tsx - app/(tabs)/index.tsx only if needed - app/friend/[id].tsx only if needed for param handling consistency - one tiny shared helper/component only if truly useful - no unrelated screen rewrites Primary objectives A. Add a visible accepted-friends list 1. Add a clear “Friends” section somewhere appropriate, preferably on Profile. 2. It should list accepted friends only. 3. Each row/card should be tappable. 4. Tapping a friend should navigate to app/friend/[id].tsx. B. Make Friend Detail reachable in a clean, normal way 1. Navigation to Friend Detail should not depend on obscure entry points. 2. Pass the params Friend Detail needs consistently. 3. Ensure the screen can render names correctly using the existing display-name system. C. Keep the list useful but restrained 1. Show enough identity to feel human: - display name - optionally username secondary if needed - avatar initial 2. Do not turn this into a big social dashboard. 3. Keep the UI premium and lightweight. D. Preserve existing friendship flows Do not break: - find/search/add friend flow - pending friend requests - accept flow - existing friend detail logic - recommendation flows - profile stats - activity feed E. Empty states 1. If the user has no accepted friends, show a clear, elegant empty state. 2. The copy should suggest how to add friends, without being noisy. F. Implementation guidance 1. Reuse existing friendship queries if sensible. 2. Prefer the smallest safe implementation. 3. If Profile already has friend data available or can load it cheaply, use that. 4. Keep the code explicit and easy to validate. 5. Do not redesign Friend Detail itself in this pass unless a tiny consistency fix is required for navigation params. Important This pass is about accessibility/reachability of the social trust layer, not adding new social features. After editing, print only: 1. files changed 2. full contents of all changed files 3. diff for those files only 4. exact manual test steps 5. where the new friend list lives and why 6. how Friend Detail routing now works