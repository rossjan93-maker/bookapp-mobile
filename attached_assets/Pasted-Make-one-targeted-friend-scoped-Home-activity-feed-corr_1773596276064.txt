Make one targeted “friend-scoped Home activity feed correctness fix” pass. Do not change package versions. Do not add packages. Do not change schema. Do not refactor unrelated business logic. Do not broaden scope into a Home redesign. Do not break recommendation, friendship, activity, pacing, or book-detail flows. Goal: Fix Home so the main Activity feed only shows activity from accepted friends, not all users globally. Problem: The audit found that app/(tabs)/index.tsx currently loads activity_events globally with no friend filter. That means users can see activity from all users in the database, which is both a UX relevance issue and a trust/privacy issue. Scope: Only inspect and edit what is necessary, primarily: - app/(tabs)/index.tsx - one tiny helper only if truly needed - no other screens unless absolutely necessary Requirements A. Friend-scope the Activity feed 1. The Activity section on Home must only show events created by accepted friends. 2. Do not show non-friend activity. 3. Preserve ordering by newest first. 4. Preserve the existing event rendering style unless a tiny adjustment is needed for clarity. B. Use existing friendship data if possible 1. The screen already loads friendships. 2. Reuse that accepted-friend data to scope the feed. 3. Prefer the smallest, safest implementation. 4. If the best path is: - query friend IDs first, then query activity_events by actor_id in friendIds do that. 5. Do not leave filtering half client-side if the DB query can be narrowed safely. C. Handle empty states correctly 1. If the user has no accepted friends, the Activity section should show a clean empty state explaining that activity appears after adding friends. 2. If the user has friends but they have no activity yet, show a different empty state. 3. Keep both elegant and concise. D. Preserve existing Home modules Do not break: - Continue Reading - New from Friends - Find Friends - friend search / add flow - book detail routing - any pacing display already on Home E. Keep this pass narrow 1. Do not redesign Home broadly. 2. Do not add new features. 3. Fix correctness first. Implementation guidance 1. Inspect current loadFeed() and loadFriendships() behavior carefully. 2. Ensure only accepted friendships count. 3. Ensure actor_id filtering uses the actual accepted friend IDs, not usernames. 4. If there are no friend IDs, skip the activity_events query entirely and set a proper empty state. 5. Keep the code explicit and easy to validate. After editing, print only: 1. files changed 2. full contents of all changed files 3. diff for those files only 4. exact manual test steps 5. root cause found 6. how the friend scoping now works