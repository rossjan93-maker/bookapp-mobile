his new datapoint changes the diagnosis. Important observation: If I hard close the app and reopen via a fresh QR scan, onboarding works for the user. That means: - the user/session/profile/onboarding data is not fundamentally broken - cold-start bootstrap works - the bug is specifically the immediate post-verification callback path So this is NOT a general onboarding failure anymore. It is a warm-start race condition after tapping the Supabase verification link. ================================================== UPDATED DIAGNOSIS ================================================== The likely issue is: 1. auth callback opens the app 2. exchangeCodeForSession succeeds 3. the app starts routing immediately 4. session/profile/onboarding bootstrap is not fully settled yet 5. the app falls into tabs/empty state 6. on a later cold restart, bootstrap runs cleanly and onboarding appears ================================================== WHAT I WANT YOU TO INVESTIGATE ================================================== Focus specifically on the path after auth/callback succeeds. I want instrumentation around: [AUTH_CALLBACK] code exchange start [AUTH_CALLBACK] code exchange success [AUTH_CALLBACK] session now available? [AUTH_CALLBACK] profile bootstrap start [AUTH_CALLBACK] profile bootstrap success/failure [AUTH_CALLBACK] onboarding decision resolved? [AUTH_CALLBACK] routing to onboarding [AUTH_CALLBACK] routing to tabs [AUTH_CALLBACK] waiting state active ================================================== REQUIRED BEHAVIOR ================================================== After the email verification link is tapped, the callback screen should NOT hand off immediately if the app is still resolving: - session - profile row - onboarding_completed - local onboarding state Instead: - keep the user on a callback/loading screen - explicitly wait for the bootstrap decision - only then route to onboarding or tabs Unknown state must not be allowed to fall through to the tab shell. ================================================== LIKELY FIX DIRECTION ================================================== I want you to evaluate whether the callback route should: 1. exchange the code 2. explicitly await a fresh session read 3. explicitly await profile/bootstrap resolution 4. explicitly compute the onboarding decision 5. then route rather than relying on loosely-coupled effects / auth state listeners / guard logic racing each other. If the existing root guard and callback route are both trying to own this transition, simplify ownership. ================================================== SUCCESS CRITERIA ================================================== When a brand-new user taps the verification link: - app opens - callback screen shows a loading state - session exchange finishes - bootstrap resolves - onboarding loads immediately - no empty tabs state - no need to fully kill and reopen the app