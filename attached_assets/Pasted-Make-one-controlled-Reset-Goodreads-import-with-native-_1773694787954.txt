Make one controlled “Reset Goodreads import with native-state preservation” pass. Do not change package versions. Do not add packages unless absolutely necessary. Do not refactor unrelated recommendation, pacing, profile, Home, or general library logic. Do not break Goodreads import, ratings, reviews, progress, recommendations, activity, or book detail flows. Goal: Add a safe Goodreads reset capability that lets a user clear Goodreads-imported data and reimport cleanly, while preserving meaningful native in-app actions and allowing those native details to survive or be reintroduced on a later reimport. This is NOT a general “delete my data” feature. This is a scoped Goodreads import reset tool. Core product rule: Reset the Goodreads-imported layer, not the user’s native in-app layer. Scope: Inspect and edit only what is necessary, likely: - app/import/goodreads.tsx or settings/import UI - new helper/service for Goodreads reset logic - maybe import execution/merge helpers if needed - schema only if absolutely necessary - no unrelated broad screen rewrites Primary product objectives A. Add a Goodreads reset entry point Requirements: 1. Add a clearly scoped reset affordance in the Goodreads import area. 2. Label it explicitly, such as: - Reset Goodreads import - Start Goodreads import over 3. Do NOT label it as a generic delete-all-data action. B. Make the reset confirmation explicit Requirements: 1. Before destructive action, show clear confirmation text. 2. Explain that the reset will: - remove Goodreads-imported data/provenance - preserve native in-app actions where possible - allow a fresh reimport 3. Make the warning strong but clear. 4. Keep it premium and readable, not scary or technical. C. Preserve native in-app actions Requirements: 1. Goodreads reset should not blindly delete all user_books that originated from Goodreads. 2. If a Goodreads-imported book has meaningful native activity, preserve that native layer. 3. Examples of native activity to preserve: - progress/current_page updates made in-app - native rating or rerating - native review/note edits - recommendation relationships / source relationships created in-app - activity generated from native app behavior - later status changes made in-app 4. The reset should not wipe valuable native history. D. Support “purely imported” vs “native-touched” behavior Requirements: 1. For imported rows with no meaningful native activity: - it is acceptable to remove them cleanly 2. For imported rows with native activity: - preserve the native state - remove or clear Goodreads-specific import provenance/state as appropriate - keep the row usable for a later reimport/merge E. Reimport-safe merge behavior Requirements: 1. If the same book returns on a later Goodreads reimport, preserved native state should win where appropriate. 2. Goodreads reimport should restore imported context without overwriting newer native truth. 3. Native-wins rules should apply to fields like: - current_page / progress - status if changed natively after import - rating if changed natively after import - review_body / private_note if edited natively after import 4. Goodreads should fill empty or purely imported gaps, not overwrite meaningful native state. F. Preserve recommendations and social data Requirements: 1. Goodreads reset should not delete recommendation relationships created in-app. 2. It should not break activity feed history unnecessarily. 3. It should not break friendships, profile, yearly goal, or other unrelated user state. G. Clean up import provenance and staging Requirements: 1. Reset should appropriately clear Goodreads-specific import provenance, including: - import_batches - import_rows - import_source / import_source_book_id / import_batch_id where appropriate 2. Keep this bounded to Goodreads import state, not general app data. H. Keep the implementation explicit and safe Requirements: 1. Prefer a dedicated Goodreads reset helper/service. 2. Keep rules field-aware and conservative. 3. Do not make this a blunt delete query. 4. Favor correctness over cleverness. Implementation guidance 1. Distinguish clearly between: - imported-only state - native-touched state 2. If needed, define practical heuristics for “native-touched” vs “purely imported.” 3. A reasonable native-touch heuristic may include: - current_page not null or changed in-app - recommendation linkage exists - rating/review differs from imported source - activity/progress events exist 4. Preserve rows when native activity exists; clean-remove only rows that are effectively untouched imported records. 5. Keep this focused on Goodreads import reset, not generic account deletion. Desired UX - user opens Goodreads import area - sees “Reset Goodreads import” - confirms with explicit explanation - reset runs - imported Goodreads layer is cleared/repaired - user can immediately run a fresh Goodreads import again - native in-app value is still there where it should be After editing, print only: 1. files changed 2. full contents of changed files 3. the diff for those files only 4. exact manual test steps 5. reset rules used 6. native-preservation rules used 7. reimport merge rules used 8. what assumptions were made to distinguish imported-only vs native-touched rows