Make one targeted “single-book forensic audit for missing metadata” pass using this exact book: Book: Glow (The Plated Prisoner, #4) Author: Raven Kennedy Current observed behavior: - The book exists in the imported Goodreads library - Book Detail shows: - title - author - finished status - Your History (rating + finished date) - But it does NOT show: - cover artwork - description/about - subjects/genres This is now a forensic debugging pass for one concrete imported book. Do not do another broad speculative metadata pass. Find the exact break for this book and fix it. Constraints: - Do not change package versions - Do not add packages unless absolutely necessary - Do not refactor unrelated profile, Home, recommendation, pacing, or social logic - Do not broaden this into another general redesign - Keep the fix surgical and evidence-based Scope: Inspect only what is necessary, likely: - app/book/[id].tsx - lib/metadataRepair.ts - lib/openLibrary.ts - lib/googleBooks.ts - maybe app/(tabs)/library.tsx only if needed for local-state refresh - maybe one tiny helper if necessary - no unrelated files unless truly required Primary objectives A. Trace this exact book end-to-end For Glow (The Plated Prisoner, #4), determine: 1. what `book_id` / `books` row is being used 2. whether that row has: - external_id - isbn - isbn13 - cover_url - description - subjects - page_count 3. whether the metadata repair path is actually selecting this book 4. whether Open Library / Google Books lookups are being attempted for it 5. whether those lookups return usable data 6. whether the DB update succeeds 7. whether the Book Detail UI reads the persisted values correctly B. Find the exact root cause for THIS book In your response, explicitly state the single concrete reason this specific book is still blank. Examples: - no usable lookup identifiers on the `books` row - lookup helper returns no result for this title - fallback query is malformed - update patch is never sent - DB update succeeds but UI is not reading the fields Do not respond with vague “improved metadata handling.” C. Fix the actual issue Apply the smallest correct fix that resolves the real failure for this book. Do not stack speculative fallback layers unless needed. D. Use this book as the validation case After the fix, the expected outcome for Glow is: - cover appears in Book Detail and Library if a valid cover can be found - description/about appears if available from a trusted source - subjects/genres appear if available from a trusted source - if any of these cannot be found for this title, say exactly why E. Be explicit about source limitations If this exact book cannot get one of these fields because the external source does not provide it, say so clearly. Distinguish between: - code/path bug - missing source data - missing identifiers - UI read/render issue Implementation guidance 1. Start from the specific `books` row for this title. 2. Trace the real code path that runs when this book detail page opens. 3. Verify whether the metadata services are returning data for this specific title. 4. Fix only what is necessary. 5. Keep the answer conclusive. After editing, print only: 1. files changed 2. full contents of changed files 3. the diff for those files only 4. exact manual test steps using Glow (The Plated Prisoner, #4) 5. the exact root cause found for this book 6. the exact fix applied 7. whether the issue was identifier quality, lookup failure, DB persistence, or UI read/render 8. any remaining limitation for this specific title