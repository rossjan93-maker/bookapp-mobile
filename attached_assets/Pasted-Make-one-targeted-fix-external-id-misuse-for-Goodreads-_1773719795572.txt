Make one targeted “fix external_id misuse for Goodreads-imported books” pass. Do not change package versions. Do not add packages unless absolutely necessary. Do not refactor unrelated profile, Home, recommendation, pacing, or social logic. Do not broaden this into another generic metadata pass. Known concrete failure case: For the imported book: Glow (The Plated Prisoner, #4) — Raven Kennedy The books row currently looks like: - external_id = goodreads:58159316 - isbn = null - isbn13 = null - cover_url = null - description = null - subjects = null This is now the key issue. Goal: Fix the misuse of books.external_id so Goodreads-imported books can hydrate correctly. Product rule: - books.external_id should represent an Open Library work identifier (or other app-supported canonical metadata ID), not a Goodreads source id string - Goodreads source ids should live in provenance/link tables, not in books.external_id Scope: Inspect and edit only what is necessary, likely: - Goodreads import execution / matching logic - metadata repair logic - app/book/[id].tsx if needed - lib/openLibrary.ts - maybe a repair migration/helper for existing bad rows - no unrelated broad rewrites Primary objectives A. Identify where goodreads:{id} is being written into books.external_id Requirements: 1. Find the exact code path that writes Goodreads source ids into books.external_id. 2. Stop doing that. 3. Preserve Goodreads source ids in the correct provenance location instead: - book_source_links - import_source_book_id - or equivalent existing provenance field B. Make metadata hydration ignore non-OL external_id values Requirements: 1. If books.external_id is not an Open Library works id (e.g. does not look like /works/OL...), it must NOT be treated as a valid OL identifier. 2. In that case, metadata repair should fall back to: - searchOLWork(title, author) - isbn13 / isbn - Google Books title+author fallback 3. Do not let a Goodreads-prefixed id block hydration. C. Repair existing bad rows Requirements: 1. Add a bounded repair path for existing books whose external_id is a Goodreads-style value. 2. For such rows: - do not rely on external_id for OL fetches - attempt to find a real OL work id by title+author - if found, persist the OL work id into books.external_id 3. Keep Goodreads provenance elsewhere intact. D. Strengthen import-time identifier handling Requirements: 1. If Goodreads CSV provides ISBN/ISBN13, ensure those actually get populated into books where appropriate. 2. If they are absent, fall back cleanly to title+author search. 3. Do not leave a row in a state where: - external_id is a Goodreads id - and isbn/isbn13 are null - and no metadata lookup can succeed E. Validate with this exact title Requirements: 1. Use Glow (The Plated Prisoner, #4) as the concrete validation case. 2. After the fix, this row should ideally become: - external_id = /works/OL... - description populated if OL has it - cover populated if GB or other source has it - subjects populated if OL has them 3. If some field remains unavailable due to source-data limitations, say so explicitly. After editing, print only: 1. files changed 2. full contents of changed files 3. the diff for those files only 4. exact manual test steps for Glow 5. the exact root cause found 6. the exact fix applied 7. whether the issue was bad identifier storage, lookup gating, or persistence 8. any remaining limitation for this title