Make one targeted “finish Glow metadata completion” pass for this exact book: Glow (The Plated Prisoner, #4) Author: Raven Kennedy Current confirmed DB state: - external_id = /works/OL27811644W - subjects = populated - page_count = populated - cover_url = null - description = null Important: This means the earlier identifier problem is already solved. Do NOT do another broad metadata pass. Do NOT redesign the importer. This is now a narrow forensic fix for the last two missing fields on this one book: 1. cover_url 2. description Goal: Determine exactly why Glow still does not get cover_url and description even though it now has a valid Open Library work id and other metadata is persisting. Scope: Inspect only what is necessary, likely: - lib/openLibrary.ts - lib/googleBooks.ts - lib/metadataRepair.ts - app/book/[id].tsx - maybe one tiny helper if necessary - no unrelated broad rewrites Primary objectives A. Trace the OL description path for this exact work Requirements: 1. Use external_id = /works/OL27811644W as the starting point. 2. Verify what fetchOLMeta returns for description for this exact work. 3. If description exists in a different OL shape/location than expected, fix the parser. 4. If description truly does not exist in the OL work response, say so explicitly. B. Trace the cover path for this exact work Requirements: 1. Determine why cover_url is still null. 2. If the work has no direct cover, inspect whether edition-level OL data exposes ISBNs or cover-capable edition identifiers. 3. Use those edition-level identifiers to improve cover lookup if appropriate. 4. If Google Books can find a cover once edition ISBNs are extracted, persist it. 5. If no reliable cover exists from available sources, say so explicitly. C. Be explicit about the exact remaining failure Requirements: 1. In the response, state the exact reason description is still null. 2. State the exact reason cover_url is still null. 3. Distinguish clearly between: - parser issue - source-data limitation - edition-vs-work lookup issue - persistence issue D. Fix only what is needed Requirements: 1. Keep the fix narrow and evidence-based. 2. Do not add more speculative fallback layers unless needed for this exact title. 3. Preserve the now-correct OL external_id already stored. Validation target After the fix, Glow should ideally have: - description populated - cover_url populated If one of those still cannot be populated, explain exactly why. After editing, print only: 1. files changed 2. full contents of changed files 3. the diff for those files only 4. exact manual test steps for Glow 5. exact root cause for missing description 6. exact root cause for missing cover 7. exact fix applied 8. whether the limitation is parser, edition lookup, source data, or persistence