We need to stop patching isolated auth/onboarding bugs and fix the entire first-run entry system end to end. I have not seen the correct play working in any version. I want a clean walkthrough of ALL sign-up / auth entry paths and I want every one of them to adhere to the same onboarding sequence. ================================================== AUTHORITATIVE FIRST-RUN SEQUENCE ================================================== For a truly new user, the flow must always be: 1. Sign up 2. Welcome 3. Guided app walkthrough 4. Final setup - Upload / import library (primary) - Add books manually (secondary) - Not right now (tertiary) 5. Normal app usage This must be true regardless of how the user enters the app. The user must never: - land directly in the app shell on first run - land directly in taste questions first - see import/setup as the first screen - skip final setup after walkthrough - get trapped in setup with no clean escape - see different onboarding sequences depending on auth path ================================================== I WANT A FULL AUTH / ENTRY PATH AUDIT ================================================== Before coding anything, I want you to trace and document every relevant entry path. Audit these exact paths: A. Brand-new signup with email/password, before email confirmation B. Brand-new signup, then click Supabase confirmation email link C. Brand-new signup, email confirmed, then first login from the login form D. Returning user login after onboarding is already completed E. User refreshes during welcome F. User refreshes during walkthrough G. User refreshes during final setup H. User dismisses final setup, logs out, logs back in I. User taps import, leaves mid-flow, comes back J. Deleted account recreated with same email on same device/browser K. Deleted account recreated with same email on a clean browser/incognito session For each path, tell me: 1. exact first route/screen reached 2. exact auth/session event path 3. exact onboarding/profile/local-state values read 4. exact route decision 5. whether the result matches the intended onboarding sequence or not ================================================== AUTHORITATIVE STATE MODEL ================================================== I want one clear explanation of the current source of truth for: - profile/backend durable onboarding state - local onboarding stage - walkthrough sub-step state - final setup pending/dismissed/completed state - any local auth-confirm / redirect state - any stale local state that can survive account deletion and corrupt a fresh signup I want you to identify: - which state is authoritative - which state is temporary - which state is legacy / redundant / dangerous - which state is currently causing drift If the current system still has multiple drifting onboarding/auth keys, call that out directly. ================================================== SIGNUP FORM REQUIREMENTS ================================================== Also fix the signup form while you are doing this pass. Requirements: - add Confirm Password field - add show/hide password behavior - validate password + confirm password match before submit - keep the styling premium, minimal, and production-grade Before coding, tell me: 1. exact current signup form structure 2. exact files/components to modify 3. how mismatch validation will work 4. how show/hide will work ================================================== FINAL SETUP REQUIREMENTS ================================================== Final setup must be the same across all new-user paths. It should clearly prioritize: 1. Upload / import library 2. Add books manually 3. Not right now Quick taste questions can exist, but they are not allowed to hijack the first-run sequence again. The user must be able to leave this step cleanly. If they dismiss it, do not immediately bounce them back into it. If they import successfully, do not show it again. If they refresh while it is pending, they should return to that same final setup step. ================================================== EMAIL CONFIRMATION PATH ================================================== This is especially important: The email confirmation link path must use the same onboarding decision logic as all other entry paths. A brand-new verified user must not be dumped straight into the app. If `profiles.onboarding_completed = false` and there is no valid completed onboarding state, they must go into: welcome -> walkthrough -> final setup I want you to trace that exact confirmation-link path separately and prove it works. ================================================== DELETION / RE-CREATION REQUIREMENTS ================================================== Because I am using account deletion for testing, I need you to verify that: - deleting an account clears or neutralizes any local onboarding state that would corrupt a recreated account - recreating an account with the same email on the same device/browser still behaves like a new user - recreating on a clean browser session also behaves correctly ================================================== WHAT I WANT BEFORE CODING ================================================== Before implementing, give me a structured diagnosis with: 1. the full auth/onboarding path audit 2. the exact broken paths 3. the exact reasons each broken path fails 4. the authoritative corrected state/route model 5. the exact files/components/state you will modify Do not give me vague summaries. I want the actual chain for each path. ================================================== IMPLEMENTATION REQUIREMENT ================================================== Then implement the fix so that ALL new-user entry paths obey the same first-run onboarding sequence. If needed: - unify route guards - delay routing until profile/bootstrap state is known - create or fix a dedicated auth-confirm route - clean up stale local onboarding keys on delete/sign-out/recreate - remove legacy onboarding state that causes drift ================================================== RUNTIME PROOF REQUIRED ================================================== After implementing, provide proof for each audited path: - exact logs - exact route taken - exact state read/written - final screen reached I want a clear pass/fail matrix for: A through K ================================================== SUCCESS CRITERIA ================================================== - all brand-new user entry paths produce the same onboarding sequence: sign up -> welcome -> walkthrough -> final setup -> app - confirm-email path does not bypass onboarding - signup form has confirm password + show/hide - final setup prioritizes import/upload and add books - users are not trapped in setup - refresh resumes the correct onboarding stage - returning users do not re-enter onboarding - deleted/recreated users still get clean first-run onboarding - no more path-specific regressions