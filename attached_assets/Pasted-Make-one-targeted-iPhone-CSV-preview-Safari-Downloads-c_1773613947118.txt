Make one targeted “iPhone CSV preview / Safari Downloads clarification” pass. Do not change package versions. Do not add packages. Do not change schema. Do not refactor parser, staging, or execution logic. Do not break the current Goodreads import flow. Goal: Fix the Goodreads mobile import instructions so they accurately reflect the real iPhone behavior we observed during testing. Observed behavior on iPhone: - after Goodreads export is triggered, the CSV may open directly in a viewer/preview - the user does NOT necessarily see a clear “Save to Files” or “Download” option - the current Step 4 guidance is therefore misleading Current incorrect guidance: “tap Share → Save to Files (or Downloads)” This should no longer be the default iPhone instruction. What the instructions should communicate instead: 1. Goodreads may quietly generate the export 2. tapping the file may open a CSV preview on iPhone 3. that preview is not the failure state 4. the user should close the preview, return to Safari, and look for the file in Safari’s Downloads area 5. then they should come back to our app and choose the CSV from the file picker Scope: Only inspect and edit what is necessary, likely: - app/import/goodreads.tsx - no unrelated logic changes Requirements A. Rewrite Step 4 for iPhone realism Requirements: 1. Remove the default “Share → Save to Files” guidance. 2. Replace it with clearer iPhone-specific guidance: - if the CSV opens in a preview, close it - go back to Safari - find the file in Safari Downloads / recent downloads - return here and choose the CSV file 3. Keep the wording human and explicit. B. Clarify Step 3 Requirements: 1. Make it clearer that Goodreads may be very subtle after Export Library is tapped. 2. Explain that the export may: - quietly add a line under the button - or open the CSV immediately 3. Reassure the user that this can still mean success. C. Keep the black button tied clearly to Step 1 Requirements: 1. Preserve the explicit relationship between Step 1 and the black “Open Goodreads Export Page” button. D. Keep the UI calm Requirements: 1. Do not turn the screen into a giant help page. 2. Use short, very clear copy. 3. Keep the premium editorial feel. E. Preserve all current functionality Do not break: - Open Goodreads Export Page button - Choose CSV File button - web/mobile branching - parser/staging/execution flow Implementation guidance 1. This is a copy and instructional-clarity pass. 2. Optimize around the tested iPhone behavior, not an assumed generic download flow. 3. The user should understand that a CSV preview opening is normal. 4. The user should know to retrieve the actual file from Safari Downloads, then upload it here. After editing, print only: 1. files changed 2. full contents of changed files 3. the diff for those files only 4. exact manual test steps 5. iPhone-specific instruction changes made 6. how Step 3 and Step 4 now reflect the observed CSV preview behavior