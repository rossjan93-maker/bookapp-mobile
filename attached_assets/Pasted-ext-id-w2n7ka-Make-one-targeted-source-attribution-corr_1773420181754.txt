ext id="w2n7ka" Make one targeted “source attribution correction” pass. Do not change package versions. Do not add packages. Do not refactor unrelated logic. Do not change progress history or sentiment behavior. Do not touch pacing logic unless absolutely necessary. Goal: Fix source attribution so future analytics are trustworthy. Problem: The recent reader-signals pass added user_books.source with DEFAULT 'self_added', and new recommendation saves now write source = 'recommendation'. But this likely leaves two data-integrity problems: 1. Historical recommendation-derived user_books rows may still be mislabeled as self_added 2. If notes.tsx reuses an existing user_books row when saving a recommendation, that reused row may not get corrected to source = 'recommendation' This would distort: - self_added vs recommendation analysis - recommendation conversion - future trust / taste-fit signals Scope: Only inspect and edit what is necessary: - supabase/migrations/20260313000002_reader_signals.sql if needed - add one NEW migration if cleaner - app/(tabs)/notes.tsx - lib/signals.ts only if necessary for correctness - no other files unless absolutely required Requirements: A. Historical backfill 1. Add the smallest safe DB correction so existing recommendation-derived user_books rows are marked correctly. 2. Preferred rule: - if a recommendations row has user_book_id = user_books.id - then user_books.source should be 'recommendation' 3. This should be implemented as a migration / SQL backfill, not left as a manual assumption. 4. Keep it idempotent if possible. B. Reused existing user_books rows 1. In notes.tsx, if a recommendation save flow reuses an existing user_books row for the same book/user, make sure that row is corrected to: - source = 'recommendation' 2. Do this without breaking current save behavior. 3. Preserve pre-migration defensive fallback behavior if possible. C. Signals correctness 1. Inspect lib/signals.ts only if needed. 2. Ensure recommendation conversion logic is based on correct data and not on stale assumptions. 3. Do not add new metrics in this pass. D. Preserve all current flows Do not break: - inbox save flow - recommendation lifecycle - library - progress logging - sentiment capture - profile - activity feed Implementation guidance 1. Prefer a new migration rather than rewriting the prior migration if that is cleaner. 2. The migration should do two things if needed: - backfill old rows - optionally tighten/update defaults only if necessary 3. Keep the app-side change in notes.tsx minimal and explicit. 4. No UI changes are needed. After editing, print only: 1. files changed 2. full contents of all changed files 3. the diff for those files only 4. exact manual test steps 5. root cause fixed 6. whether historical backfill was added